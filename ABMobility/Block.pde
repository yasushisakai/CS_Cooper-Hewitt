/*
The Block class is the virtual representation of the physical lego
building blocks on the table.

Blocks have dynamic locations determined by where the user has placed
them.  If not placed on the table, they have a default location in
"zombie land".
*/

public class Block {
    int id; // ID of block, determined by spreadsheet data.

    // location (TODO)
    // default_location (TODO)
    
    // orientation ?  To be implemented later.
}
