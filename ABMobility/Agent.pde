


public class Agent {
    /* Each agent is assigned a home block, a work block, and
    a motif.  These are preassigned based on the predetermined
    blocks on the table. 
    The agent determines mobility choice dynamically based on
    distance between home and work, and the world the agent is in.

    TODO: say more about this.

    */

    Block homeBlock;
    Block workBlock;
    // TODO: add motif


    Agent(int homeBlockId, int workBlockId) {
        // TODO: instantiate the agent with blocks based on IDs.

    }
}